# QingYan 产品设计规范

本文档是 QingYan 产品信息架构、交互、文案和保存语义的唯一规范。它描述产品应该如何工作；开发过程、测试和 Git 交付规则由 `AGENTS.md` 管理。

## Admin Console 定位

QingYan Admin Console 是运维和内容管理后台，设计目标是克制、高密度、可扫描、可重复操作。功能完整不等于把所有控件平铺出来；页面必须按真实工作流组织信息，不能让数据库结构、后端模块或实现便利决定用户操作顺序。

## 信息架构与设置归属

- 页面应先展示用户要管理的业务对象，再展示对象的参与者和实现方式。父级能力不能依赖子项存在才显得“完整”；没有子项时应呈现明确空态或“不执行”状态。
- 设置页和管理页按语义与操作流组织。配置项及依赖它的测试、预览、校验必须处于同一上下文；例如邮件服务器测试属于系统邮件设置，不应拆到另一个通知页面。
- 同一份配置只能有一个明确 owner。系统设置负责实例共用能力，站点设置负责当前站点如何使用这些能力；站点页面可以展示系统能力的只读状态和跳转入口，但不得复制一套可编辑配置。
- 跨页面依赖必须单向、可解释：上游定义能力，下游选择是否使用。页面不能相互修改对方的配置，也不能要求用户在两个页面来回比对才能判断最终是否生效。
- 新增或重构页面前，先判断它属于“单对象设置”“多对象管理”“模板/编辑器”“任务/结果列表”，再选择布局；不能为了实现方便把所有字段直接平铺到当前页面。

### 通知设置的产品层级

- 系统设置负责实例级发送能力：邮件服务器、外部发送目标、发送保护与全局模板。
- 站点设置负责站点级通知意图：什么事件需要通知、分别通知给谁。
- 后台评论通知以事件为父级。目前固定包含“新待审评论”和“直接发布评论”；两个事件始终存在，各自独立选择接收人。
- 接收人不是通知链路的父级。某个事件没有接收人时，含义是“当前不会发送”，属于合法状态，不应显示为链路缺失或系统故障。
- 后台用户接收人通过可搜索、多选的控件直接配置。不得要求用户先创建接收人，再进入接收人弹窗理解或配置内部 route、事件代码和发送链路。
- Webhook/WxPusher 等外部目标属于事件级接收目标，不应通过虚构或绑定某个后台用户来完成发送。
- 发送前检查只判断已保存配置。未保存更改必须明确标示；真实测试依赖已保存配置时，应直接说明并提供可见的保存入口。

## 用户文案与错误恢复

- 所有用户可见文案只说明用户需要完成的操作、当前状态、结果和恢复路径。
- 不得把后端流程或内部标识直接展示给用户，包括但不限于 `route`、worker、队列后端、字段路径、事件代码、任务/投递 ID、服务商消息 ID、异常类型和调试日志。
- 配置页面确实需要用户理解的产品概念，应改写为对应的业务名称和操作名称。
- 后端诊断与错误可以保留稳定错误码、内部 message/path 和调试详情，但 Admin UI 不得直接渲染这些后端文案。
- 前端必须按稳定错误码映射为场景化说明，明确告诉用户“到哪里、修改什么、保存后如何重试”。未知错误码使用不泄露内部信息的通用恢复提示，详细信息只留在服务端日志和运维诊断中。
- 状态标记和操作按钮不得被动态文案挤压或推离可视区域。不定长文本容器必须允许收缩和换行，状态标记与固定操作保持不收缩；不能依赖后端文本长度恰好适配当前布局。

## 保存模型与事务语义

一个操作流只能选择一种保存模型。产品不得混用“弹出层已经保存”的交互语义与“整页稍后统一提交”的实现。

### 独立弹出层

- 用于新增或编辑一个完整配置对象的弹窗、抽屉或侧边层，是独立、完整的配置事务。
- 点击“保存/确认”必须立即发起真实持久化请求；只有服务端成功后才能关闭并更新页面。
- 请求失败时保留用户输入和弹出层，展示可恢复提示；取消不得产生空数据。
- 禁止把“保存/确认”实现成只修改页面内存草稿、关闭弹层后再依赖页面另一处保存按钮。
- “已保存/修改成功”反馈只能来自服务端成功响应。刷新或重新读取后必须得到相同配置。

### 整页草稿与批量应用

- 只有需要跨多个控件统一校验、批量预览或原子提交时，才使用整页草稿模式。
- 从第一次改动开始，页面必须常驻显示未应用状态和准确的改动数量。
- “应用全部更改”和“放弃更改”必须在滚动过程中始终可见，可使用吸顶/吸底操作条、侧边常驻面板或等价控件。
- 页面级保存入口不得藏在长页面底部、折叠内容或其它页签之后。
- 如果草稿模式中必须打开辅助弹层，主按钮必须写成“加入待应用更改”等真实语义，不能写“保存/确认”，也不能显示保存成功。
- 切换页签、关闭设置面板、离开页面或刷新前，必须阻止无提示丢失未应用更改。
- 纯前端状态变化只能提示“有未应用的更改”，不得伪装成持久化成功。

## 表单、列表与弹出层

- 多条配置数据默认采用“列表/表格 + 添加按钮 + 独立弹出层表单”。典型对象包括通知渠道、黑名单规则、模板变体和外部来源。
- 如果列表已经完整展示所有可编辑字段，且字段少、无密钥、无条件分支、无复杂校验，可以行内编辑；否则使用独立弹出层。
- 包含密钥、Webhook/WxPusher 配置、条件字段、长文本、模板或说明性校验的对象默认使用独立弹出层。
- 低频操作不要常驻平铺。测试、恢复默认、危险删除和批量影响较大的操作放在列表行或分组头部，通过弹层或确认层收集必要参数并提交。
- 列表项承担主要浏览信息：名称、类型、状态、目标摘要、密钥是否已配置、最近结果和关联数量应尽量可见；操作区只放查看、编辑、测试、删除等明确动作。
- 模板编辑按事件和语言/场景选择模板，围绕当前模板展示说明、可用占位符、主题/正文、实时预览、恢复默认、保存和测试发送，不能拆成互相割裂的页面。

## 布局与信息密度

- 表单布局不要机械使用固定两栏。字段少或一侧留空时，应合并分组、改为单列/紧凑行或使用列表/表格。
- 宽屏下输入框、选择器和文本域必须有合理最大宽度，不能随 viewport 无限拉伸。
- 设置项密度要均衡。普通开关、短输入、数字限制和枚举选择应在同一语义组内紧凑排列；只有长说明、编辑器、预览或复杂配置才占整行。
- 页面级 section 可以有边界和标题，但设置页主体避免卡片套卡片和大片空白。卡片主要用于单条列表项、弹层内容或需要明确框定的工具面板。
- 宽屏优先使用受控内容宽度、合理 grid tracks、`max-width`、紧凑表格和左右工作区；窄屏自然折叠为单列。

## 测试与结果反馈

- 邮件、Webhook、WxPusher 等发送测试应贴近对应的系统发送能力；测试依赖已保存配置时，有未应用更改必须禁用测试并提供直接可见的应用入口。
- 任何测试结果都只陈述用户可以确认的事实。例如“邮件服务商已接受”不等于“收件箱已经收到”，界面应继续提示核对收件箱、垃圾邮件和退信。
- 操作完成后，列表、状态、诊断和测试必须基于服务端返回或重新读取的数据更新，不能仅凭本地乐观状态宣布完成。
